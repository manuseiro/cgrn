# GlebasNord - Configuração de Erros
ErrorDocument 404 /cgrn/404.php

# Otimização básica
<IfModule mod_rewrite.c>
    RewriteEngine On
    # Garante que as pastas terminem com barra
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^(.*[^/])$ /$1/ [L,R=301]
</IfModule>
